import styled from "styled-components";

const pad = "10px";
export const Container = styled.form`
  display: flex;
  .ant-select {
    margin-right: ${pad};
  }
  .ant-btn {
    margin-left: ${pad};
  }
`;
